const request = require('supertest');

const app = require('../src/app')

describe('GET', () => {
  xtest('returns a array with all books for a user', () => {

  })

  xtest('returns an empty array when no books are found for a user', () => {

  })
})
